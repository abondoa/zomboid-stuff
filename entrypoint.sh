#!/bin/sh
# Runs the application in the mode specified by the MODE environment variable
# Default is discord mode
# Usage: MODE=cli (for CLI mode) or MODE=discord (for Discord mode)
node dist/index.js
